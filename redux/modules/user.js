// Imports

// Actions

// Action Creators

// API actions

// Initial state

const initialState = {
  isLoggedIn: false
};

// Reducer

function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

// Function

// Exports

// Default Reducer Export

export default reducer;
